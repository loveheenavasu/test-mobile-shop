// export const generatePublicUrl = (fileName) => {
//   return `http://localhost:5000/api/uploads/${fileName}`;
// };

export const generatePublicUrl = (fileName) => {
  return `https://mobileshop.ug/api/uploads/${fileName}`;
};